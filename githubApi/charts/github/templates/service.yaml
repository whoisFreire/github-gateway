apiVersion: v1
kind: Service
metadata:
  name: {{ template "github.fullname" . }}-{{ .Values.environment.stage }}
  labels:
    app: {{ template "github.name" . }}
    chart: {{ template "github.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "github.name" . }}
    release: {{ .Release.Name }}